/*
    Conceito de variáveis em C:

        1) Toda variável possui um nome;
        2) Toda variável possui um tipo;
        3) Toda variável possui um tamanho;
        4) Toda variável possui um valor.

        A) O valor da variável é temporário;
        B) Quando o programa finaliza, também ocorre o mesmo com o valor contido na var;
        C) Variáveis são voláteis;
        D) Uma var só recebe um valor.

    Tipos de dados:

        1) Char = 1 byte;
        2) Int = 2, 4 ou 8 bytes (entre -32758 e +32767);
        3) Float = 7 bytes (números até 7 dígitos);
        4) Void = vazio;
        5) Double = 8 bytes (números até 15 dígitos);
*/

#include <stdio.h>

int main() {
    printf("Conceitos de variáveis em C.\n");

    return 0;
}
